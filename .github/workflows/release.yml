name: Release

on:
  release:
    types: [ published ]
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    runs-on: windows-2022
    steps:
    - uses: actions/checkout@v4
    - name: Setup dotnet
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Publish
      run: |
        dotnet publish app/GHelper.sln --configuration Release --runtime win-x64 -p:PublishSingleFile=true --no-self-contained

    - name: Upload unsigned GHelper EXE
      id: upload-unsigned
      uses: actions/upload-artifact@v4
      with:
        name: GHelper-unsigned
        path: app/bin/x64/Release/net8.0-windows/win-x64/publish/GHelper.exe

    - name: Upload unsigned GHelperOverlay EXE
      id: upload-unsigned-overlay
      uses: actions/upload-artifact@v4
      with:
        name: GHelperOverlay-unsigned
        path: overlay/bin/x64/Release/net48/win-x64/GHelperOverlay.exe

    - id: sign-exe
      uses: signpath/github-action-submit-signing-request@v1.1
      with:
        api-token: ${{ secrets.SIGNPATH_API_TOKEN }}
        organization-id: ${{ secrets.SIGNPATH_ORG_ID }}
        project-slug: ${{ secrets.SIGNPATH_PROJECT }}
        signing-policy-slug: ${{ secrets.SIGNPATH_POLICY }}
        github-artifact-id: ${{ steps.upload-unsigned.outputs.artifact-id }}
        wait-for-completion: true
        output-artifact-directory: './signed'

    - id: sign-overlay-exe
      uses: signpath/github-action-submit-signing-request@v1.1
      with:
        api-token: ${{ secrets.SIGNPATH_API_TOKEN }}
        organization-id: ${{ secrets.SIGNPATH_ORG_ID }}
        project-slug: ${{ secrets.SIGNPATH_PROJECT }}
        signing-policy-slug: ${{ secrets.SIGNPATH_POLICY }}
        github-artifact-id: ${{ steps.upload-unsigned-overlay.outputs.artifact-id }}
        wait-for-completion: true
        output-artifact-directory: './signed'

    - name: Create ZIP from signed EXEs
      run: |
        powershell Compress-Archive ./signed/GHelper.exe ./signed/GHelper.zip
        powershell Compress-Archive ./signed/GHelperOverlay.exe ./signed/GHelperOverlay.zip

    - name: Upload signed EXEs and ZIPs
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        gh release upload "${{ github.event.release.tag_name }}" ./signed/GHelper.exe ./signed/GHelper.zip ./signed/GHelperOverlay.exe ./signed/GHelperOverlay.zip --clobber
    - name: Generate SLSA build provenance attestation
      uses: actions/attest-build-provenance@v2
      with:
        subject-path: |
          ./signed/GHelper.exe
          ./signed/GHelper.zip
          ./signed/GHelperOverlay.exe
          ./signed/GHelperOverlay.zip
